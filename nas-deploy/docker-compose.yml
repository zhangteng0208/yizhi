version: '3'

services:
  nginx:
    image: nginx:alpine
    container_name: yizhi-json-server
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - /volume1/docker/yizhi-data:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
